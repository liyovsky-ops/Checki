export default [
  {
    category: 'Konfiguracja & Identity',
    icon: '⚙️',
    items: [
      { cmd: 'aws configure', desc: 'Ustaw Access Key, Secret, region i format wyjścia',
        detail: { what: 'Interaktywnie zapisuje dane do ~/.aws/credentials i ~/.aws/config. Cztery pola: Access Key ID, Secret Access Key, region (np. eu-west-1), output format (json/yaml/table/text).', how: 'aws configure [--profile nazwa]', flags: [{flag: '--profile prod', desc: 'Utwórz/edytuj nazwany profil'}], tips: ['aws configure list — sprawdź aktualną konfigurację', 'export AWS_PROFILE=prod — przełącz profil dla sesji', 'aws configure sso — konfiguracja SSO / Identity Center'] } },
      { cmd: 'aws sts get-caller-identity', desc: 'Kto jestem? Account ID, ARN, User ID',
        detail: { what: 'Zwraca Account ID, ARN i UserId aktualnych credentials. Najlepszy sposób żeby sprawdzić czy CLI jest poprawnie skonfigurowane i na którym koncie operujesz.', how: 'aws sts get-caller-identity [--profile nazwa]', tips: ['aws sts get-caller-identity --query Account --output text — sam numer konta', 'Zawsze sprawdź przed wykonywaniem destrukcyjnych operacji!'] } },
      { cmd: 'aws sts assume-role --role-arn arn:aws:iam::123:role/DeployRole --role-session-name deploy-session', desc: 'Przejmij rolę IAM (cross-account lub elevated)',
        detail: { what: 'Zwraca tymczasowe credentials (AccessKeyId, SecretAccessKey, SessionToken) dla podanej roli. Używane do cross-account access i privilege escalation w bezpieczny sposób.', how: 'aws sts assume-role --role-arn [ARN] --role-session-name [nazwa]', flags: [{flag: '--duration-seconds 3600', desc: 'Czas ważności (domyślnie 1h, max 12h)'}], tips: ['Wyeksportuj zmienne: export AWS_ACCESS_KEY_ID=... AWS_SECRET_ACCESS_KEY=... AWS_SESSION_TOKEN=...', 'aws sts decode-authorization-message — zdekoduj "Access Denied" błąd do czytelnej formy'] } },
    ]
  },
  {
    category: 'S3 — Object Storage',
    icon: '🪣',
    items: [
      { cmd: 'aws s3 ls s3://nazwa-bucketa/', desc: 'Lista plików w buckecie (bez argumentu: lista wszystkich bucketów)',
        detail: { what: 'Wyświetla obiekty w buckecie. Bez argumentu zwraca listę wszystkich bucketów konta z datami i nazwami.', how: 'aws s3 ls [s3://bucket/prefix]', flags: [{flag: '--recursive', desc: 'Wszystkie podfoldery'}, {flag: '--human-readable', desc: 'Rozmiar w KB/MB/GB'}, {flag: '--summarize', desc: 'Suma obiektów i rozmiaru na końcu'}], tips: ['aws s3 ls s3://bucket/ --recursive --human-readable --summarize', 'Wynik: data, czas, rozmiar, klucz obiektu'] } },
      { cmd: 'aws s3 cp plik.txt s3://bucket/folder/', desc: 'Kopiuj plik lokalnie ↔ S3',
        detail: { what: 'Kopiuje plik do S3, z S3, lub między bucketami. Obsługuje duże pliki przez multipart upload automatycznie (>8MB).', how: 'aws s3 cp [źródło] [cel]', flags: [{flag: '--recursive', desc: 'Kopiuj folder ze wszystkim'}, {flag: '--storage-class STANDARD_IA', desc: 'Klasa przechowywania (STANDARD/IA/GLACIER)'}, {flag: '--exclude "*.log"', desc: 'Wyklucz wzorzec'}, {flag: '--include "*.py"', desc: 'Uwzględnij wzorzec'}], tips: ['aws s3 cp s3://bucket/plik . — pobierz plik', 'aws s3 cp folder/ s3://bucket/folder/ --recursive'] } },
      { cmd: 'aws s3 sync ./dist s3://moja-strona.pl/ --delete', desc: 'Synchronizuj folder z S3 — deploy statycznej strony',
        detail: { what: 'Jak rsync — kopiuje tylko zmienione/nowe pliki. --delete usuwa z S3 pliki których nie ma lokalnie. Idealne do deploy stron statycznych.', how: 'aws s3 sync [źródło] [cel]', flags: [{flag: '--delete', desc: 'Usuń z celu pliki których nie ma w źródle'}, {flag: '--acl public-read', desc: 'Ustaw publiczny dostęp'}, {flag: '--cache-control "max-age=31536000"', desc: 'Cache nagłówki dla assets'}, {flag: '--exclude "*.DS_Store"', desc: 'Wyklucz śmieci macOS'}], tips: ['aws s3 sync s3://bucket/backup . — pobierz backup lokalnie', 'aws cloudfront create-invalidation -- po sync resetuj cache CDN'] } },
      { cmd: 'aws s3 presign s3://bucket/raport.pdf --expires-in 3600', desc: 'Wygeneruj tymczasowy link do prywatnego pliku',
        detail: { what: 'Tworzy podpisany URL ważny przez expires-in sekund. Pozwala komuś pobrać prywatny plik bez credentials i bez robienia bucketu publicznym.', how: '--expires-in [sekundy] — domyślnie 3600 (1h)', tips: ['boto3: s3.generate_presigned_url("get_object", ...) — presigned POST do uploadu', 'Presigned URL zawiera w sobie tymczasowe credentials — nie udostępniaj publiczne'] } },
      { cmd: 'aws s3 mb s3://moj-bucket-prod --region eu-central-1', desc: 'Utwórz nowy bucket S3',
        detail: { what: 'Tworzy bucket. Nazwa musi być globalnie unikalna wśród wszystkich kont AWS (nie tylko Twojego). Tylko małe litery, cyfry, myślniki.', how: 'aws s3 mb s3://nazwa [--region region]', tips: ['ZAWSZE podawaj --region — bez niego ląduje w us-east-1', 'aws s3 rb s3://bucket --force — usuń bucket z całą zawartością (nieodwracalne!)', 'Włącz wersjonowanie: aws s3api put-bucket-versioning --bucket nazwa --versioning-configuration Status=Enabled'] } },
      { cmd: 'aws s3api put-bucket-lifecycle-configuration --bucket moj-bucket --lifecycle-configuration file://lifecycle.json', desc: 'Ustaw automatyczne przenoszenie/usuwanie obiektów',
        detail: { what: 'Lifecycle rules automatycznie zmieniają klasę przechowywania lub usuwają obiekty po określonym czasie. Dramatycznie redukuje koszty.', how: 'lifecycle.json definiuje reguły: prefix, dni do przejścia na IA, Glacier, Deep Archive lub usunięcia', tips: ['STANDARD → IA po 30 dniach, → Glacier po 90 dniach, usuń po 365 dniach = klasyczny backup lifecycle', 'aws s3api get-bucket-lifecycle-configuration — sprawdź aktualne reguły'] } },
    ]
  },
  {
    category: 'EC2 — Wirtualne Maszyny',
    icon: '💻',
    items: [
      { cmd: 'aws ec2 describe-instances --query "Reservations[].Instances[].[InstanceId,InstanceType,State.Name,Tags[?Key==\'Name\'].Value|[0],PublicIpAddress]" --output table', desc: 'Lista instancji z ID, typem, stanem, nazwą i IP',
        detail: { what: 'Opisuje instancje EC2. JMESPath query filtruje tylko potrzebne pola. --output table = czytelna tabela.', how: 'aws ec2 describe-instances [--instance-ids id...] [--filters ...]', flags: [{flag: '--filters "Name=instance-state-name,Values=running"', desc: 'Tylko działające'}, {flag: '--filters "Name=tag:Env,Values=prod"', desc: 'Filtruj po tagu'}], tips: ['aws ec2 describe-instances --region eu-west-1 — inny region', 'Kolumna Tags działa tylko gdy masz tag "Name" ustawiony'] } },
      { cmd: 'aws ec2 run-instances --image-id ami-0c55b159cbfafe1f0 --instance-type t3.micro --key-name moj-klucz --security-group-ids sg-xxx --subnet-id subnet-xxx --tag-specifications \'ResourceType=instance,Tags=[{Key=Name,Value=my-server}]\'', desc: 'Uruchom nową instancję EC2',
        detail: { what: 'Tworzy i startuje instancję. Wymaga: AMI ID (OS image), typ, klucz SSH, security group, subnet.', how: 'ami-0c55b159cbfafe1f0 = Amazon Linux 2. Dla Ubuntu/Debian znajdź AMI przez aws ec2 describe-images', flags: [{flag: '--user-data file://init.sh', desc: 'Skrypt startowy (cloud-init)'}, {flag: '--iam-instance-profile Name=rola', desc: 'Rola IAM dla instancji'}, {flag: '--count 3', desc: 'Uruchom 3 instancje naraz'}], tips: ['Wolisz Launch Templates: aws ec2 create-launch-template — zapisz konfigurację raz, używaj wielokrotnie', 'aws ec2 describe-images --owners amazon --filters "Name=name,Values=al2023-ami-*" — znajdź najnowsze AMI'] } },
      { cmd: 'aws ec2 stop-instances --instance-ids i-0123456789abcdef0', desc: 'Zatrzymaj instancję (dane zachowane, billing zatrzymany)',
        detail: { what: 'Zatrzymuje instancję. Dane na EBS (dysku) są zachowane. Billing za EC2 kończy się, ale EBS nadal kosztuje. Elastic IP zachowuje opłatę gdy instancja zatrzymana.', how: 'aws ec2 stop-instances --instance-ids [id...]', tips: ['aws ec2 start-instances — uruchom ponownie', 'aws ec2 terminate-instances — USUWA instancję i dane (nieodwracalne!)', 'aws ec2 reboot-instances — restart bez utraty danych'] } },
      { cmd: 'aws ec2 create-snapshot --volume-id vol-xxx --description "backup-prod-2024-01-15"', desc: 'Utwórz snapshot dysku EBS',
        detail: { what: 'Snapshot = kopia zapasowa wolumenu EBS w S3 (zarządzane przez AWS). Inkrementalny — płacisz tylko za zmiany. Można przywrócić jako nowy wolumen lub AMI.', how: 'aws ec2 describe-snapshots --owner-ids self — lista własnych snapshotów', tips: ['aws ec2 create-image --instance-id i-xxx --name "backup-ami" — snapshot całej instancji jako AMI', 'Automatyzuj przez Data Lifecycle Manager (DLM) zamiast ręcznie'] } },
    ]
  },
  {
    category: 'Lambda — Serverless',
    icon: 'λ',
    items: [
      { cmd: 'aws lambda invoke --function-name moja-funkcja --payload \'{"user_id":42}\' response.json && cat response.json', desc: 'Wywołaj funkcję Lambda synchronicznie',
        detail: { what: 'Invoce synchronous — czeka na odpowiedź, zapisuje do pliku. Sprawdź "FunctionError" w nagłówku odpowiedzi (nie tylko status 200 który zawsze jest zwracany przy poprawnym wywołaniu).', how: 'aws lambda invoke --function-name [nazwa/ARN] --payload [JSON] [plik]', flags: [{flag: '--invocation-type Event', desc: 'Asynchronicznie (fire & forget)'}, {flag: '--invocation-type DryRun', desc: 'Tylko sprawdź uprawnienia'}, {flag: '--log-type Tail', desc: 'Zwróć ostatnie logi (base64)'}], tips: ['Zdekoduj logi: aws lambda invoke ... --log-type Tail | python3 -c "import sys,base64,json; d=json.load(sys.stdin); print(base64.b64decode(d[\'LogResult\']).decode())"'] } },
      { cmd: 'zip -r function.zip . && aws lambda update-function-code --function-name moja-funkcja --zip-file fileb://function.zip', desc: 'Spakuj i wgraj zaktualizowany kod funkcji',
        detail: { what: 'fileb:// = binary file (wymagane dla ZIP). Dla plików >50MB używaj --s3-bucket/--s3-key zamiast bezpośredniego uploadu.', how: 'aws lambda update-function-code --function-name [nazwa] --zip-file fileb://[plik.zip]', flags: [{flag: '--s3-bucket bucket --s3-key plik.zip', desc: 'Deploy z S3 (>50MB lub CI/CD)'}, {flag: '--publish', desc: 'Utwórz nową wersję (immutable, do alias)'}], tips: ['aws lambda update-function-configuration --timeout 60 --memory-size 1024 — zmień timeout/RAM bez redeploy', 'Lambda layers: aws lambda publish-layer-version --layer-name deps --zip-file fileb://deps.zip'] } },
      { cmd: 'aws logs tail /aws/lambda/moja-funkcja --follow --format short', desc: 'Obserwuj logi Lambda w czasie rzeczywistym',
        detail: { what: 'Streamuje logi z CloudWatch Logs. --follow = tail -f, --format short = bez metadata.', how: 'Log group: /aws/lambda/nazwa-funkcji', flags: [{flag: '--since 30m', desc: 'Logi z ostatnich 30 minut'}, {flag: '--filter "ERROR"', desc: 'Tylko logi zawierające "ERROR"'}], tips: ['aws logs filter-log-events --log-group /aws/lambda/fn --filter-pattern "{$.level = ERROR}" — structured filtering', 'Lambda Powertools for Python: pełne structured logging, tracing, metrics out-of-the-box'] } },
    ]
  },
  {
    category: 'IAM — Uprawnienia',
    icon: '🔐',
    items: [
      { cmd: 'aws iam create-role --role-name LambdaS3Role --assume-role-policy-document \'{"Version":"2012-10-17","Statement":[{"Effect":"Allow","Principal":{"Service":"lambda.amazonaws.com"},"Action":"sts:AssumeRole"}]}\'', desc: 'Utwórz rolę IAM dla serwisu Lambda',
        detail: { what: 'Rola = tożsamość z uprawnieniami którą serwis (Lambda, EC2, ECS) może przejąć. Trust policy (--assume-role-policy-document) określa kto może jej używać.', how: 'Krok 1: utwórz rolę (trust policy). Krok 2: dołącz permissions policy.', tips: ['aws iam attach-role-policy --role-name LambdaS3Role --policy-arn arn:aws:iam::aws:policy/AmazonS3ReadOnlyAccess', 'aws iam put-role-policy — inline policy (nie shareable, nie widoczna w liście polityk)'] } },
      { cmd: 'aws iam create-policy --policy-name AppS3Policy --policy-document file://policy.json', desc: 'Utwórz custom politykę IAM z pliku',
        detail: { what: 'Customer managed policy — możesz ją dołączyć do wielu ról/użytkowników i wersjonować.', how: 'policy.json: {"Version":"2012-10-17","Statement":[{"Effect":"Allow","Action":["s3:GetObject","s3:PutObject"],"Resource":"arn:aws:s3:::moj-bucket/*"}]}', tips: ['Principle of Least Privilege: dawaj tylko DOKŁADNIE to co potrzebne', 'aws iam simulate-principal-policy --policy-source-arn arn:... --action-names s3:DeleteObject — przetestuj czy policy blokuje', 'aws iam get-policy-version --policy-arn arn:... --version-id v1 — sprawdź treść'] } },
      { cmd: 'aws iam list-attached-role-policies --role-name MojaRola', desc: 'Sprawdź jakie polityki są dołączone do roli',
        detail: { what: 'Wyświetla managed policies dołączone do roli. Osobno sprawdź inline policies.', how: 'aws iam list-attached-role-policies --role-name [nazwa]', tips: ['aws iam list-role-policies --role-name MojaRola — inline policies (wbudowane)', 'aws iam get-role --role-name MojaRola — pełne szczegóły z trust policy', 'aws iam list-roles --query "Roles[].[RoleName,Arn]" --output table — wszystkie role'] } },
    ]
  },
  {
    category: 'ECS & ECR — Kontenery',
    icon: '🐳',
    items: [
      { cmd: 'aws ecr get-login-password --region eu-west-1 | docker login --username AWS --password-stdin 123456789.dkr.ecr.eu-west-1.amazonaws.com', desc: 'Zaloguj Docker do AWS ECR (prywatny registry)',
        detail: { what: 'ECR = Elastic Container Registry. Przed push/pull musisz zalogować Docker do ECR używając tymczasowego tokenu (12h ważności).', how: 'Token = jednorazowy hasło do Docker login. Podmień 123456789 na swój Account ID.', tips: ['aws ecr describe-repositories — lista repozytoriów', 'aws ecr create-repository --repository-name moja-app — stwórz repo', 'docker tag moja-app:latest 123456789.dkr.ecr.eu-west-1.amazonaws.com/moja-app:latest && docker push ...'] } },
      { cmd: 'aws ecs update-service --cluster prod-cluster --service api-service --force-new-deployment', desc: 'Wymuś redeploy serwisu ECS (pull nowy Docker image)',
        detail: { what: 'force-new-deployment powoduje że ECS pulling najnowszy image i restartuje taski. Bez zmiany task definition — wystarczy gdy zaktualizowałeś image w ECR.', how: 'aws ecs update-service --cluster [klaster] --service [serwis] --force-new-deployment', flags: [{flag: '--desired-count 5', desc: 'Zmień liczbę działających tasków (skalowanie)'}, {flag: '--task-definition nazwa:7', desc: 'Zmień na konkretną wersję task definition'}], tips: ['aws ecs describe-services --cluster prod --services api-service --query "services[].deployments" — sprawdź status deploy', 'aws ecs wait services-stable --cluster prod --services api-service — czekaj na ustabilizowanie'] } },
      { cmd: 'aws ecs run-task --cluster prod-cluster --task-definition db-migrate:5 --launch-type FARGATE --network-configuration "awsvpcConfiguration={subnets=[subnet-xxx],securityGroups=[sg-xxx],assignPublicIp=DISABLED}"', desc: 'Uruchom jednorazowy task (np. migracja bazy)',
        detail: { what: 'Uruchamia task ECS jednorazowo bez serwisu. Idealne do migracji DB, batch jobów, jednorazowych skryptów.', how: 'FARGATE = serverless (nie potrzebujesz EC2 nodes). assignPublicIp=DISABLED = private subnet, potrzebujesz NAT Gateway.', tips: ['aws ecs wait tasks-stopped --cluster prod --tasks [task-arn] — czekaj na zakończenie', 'aws ecs describe-tasks --cluster prod --tasks [arn] | jq ".tasks[].containers[].exitCode" — sprawdź exit code'] } },
    ]
  },
  {
    category: 'CloudWatch & Monitoring',
    icon: '📊',
    items: [
      { cmd: 'aws cloudwatch put-metric-alarm --alarm-name "prod-cpu-high" --namespace AWS/EC2 --metric-name CPUUtilization --dimensions Name=AutoScalingGroupName,Value=prod-asg --statistic Average --period 300 --threshold 70 --comparison-operator GreaterThanThreshold --evaluation-periods 3 --alarm-actions arn:aws:sns:eu-west-1:123:AlertTopic', desc: 'Utwórz alarm CPU > 70% przez 15 minut',
        detail: { what: 'Alarm monitoruje metrykę co period sekund. Jeśli threshold przekroczony przez evaluation-periods razy z rzędu — alarm ALARM state i wywołuje akcje (SNS, Auto Scaling, EC2 action).', how: 'period(300) × evaluation-periods(3) = 15 minut zanim alarm', flags: [{flag: '--ok-actions arn:aws:sns:...', desc: 'Akcja gdy alarm wraca do OK'}, {flag: '--treat-missing-data ignore', desc: 'Jak traktować brakujące dane (ignore/missing/breaching/notBreaching)'}], tips: ['Namespaces: AWS/EC2, AWS/Lambda, AWS/RDS, AWS/ApplicationELB, AWS/S3', 'aws cloudwatch set-alarm-state --alarm-name test --state-value ALARM --state-reason "test" — testuj alarm'] } },
      { cmd: 'aws logs insights query-results --log-group-name /app/prod --start-time $(date -d "1 hour ago" +%s) --end-time $(date +%s) --query-string \'fields @timestamp, @message | filter @message like /ERROR/ | sort @timestamp desc | limit 50\'', desc: 'Przeszukaj logi CloudWatch Insights (ostatnia godzina, tylko ERROR)',
        detail: { what: 'CloudWatch Logs Insights = SQL-like queries po logach. Dużo szybsze niż filter-log-events dla dużych woluminów.', how: 'aws logs start-query --log-group-name ... --start-time ... --end-time ... --query-string "..." → zwraca queryId. Potem: aws logs get-query-results --query-id [id]', tips: ['fields @timestamp, level, message | stats count(*) by level — zlicz po poziomie', 'Parsowanie JSON logów: fields @message | parse @message \'{"requestId":"*"}\' as reqId'] } },
    ]
  },
  {
    category: 'CloudFormation & CDK',
    icon: '🏗️',
    items: [
      { cmd: 'aws cloudformation deploy --template-file infrastructure.yaml --stack-name prod-stack --capabilities CAPABILITY_NAMED_IAM --parameter-overrides Environment=prod DBPassword=$(aws secretsmanager get-secret-value --secret-id db-pass --query SecretString --output text)', desc: 'Deploy infrastruktury przez CloudFormation',
        detail: { what: 'Tworzy lub aktualizuje stack. CAPABILITY_NAMED_IAM = wymagane gdy template tworzy zasoby IAM z nazwami. parameter-overrides nadpisuje parametry template.', how: 'aws cloudformation deploy --template-file [plik] --stack-name [nazwa]', flags: [{flag: '--no-execute-changeset', desc: 'Tylko stwórz change set — podgląd zmian bez wykonania'}, {flag: '--tags Env=prod Team=backend', desc: 'Taguj wszystkie zasoby w stacku'}], tips: ['aws cloudformation describe-stack-events --stack-name prod-stack — debug błędów deploy', 'aws cloudformation detect-stack-drift --stack-name prod — wykryj ręczne zmiany poza IaC', 'aws cloudformation delete-stack --stack-name prod — usuń wszystkie zasoby stacka'] } },
      { cmd: 'cdk diff && cdk deploy --require-approval never', desc: 'CDK: podejrzyj zmiany i deploy',
        detail: { what: 'AWS CDK (Cloud Development Kit) = IaC w TypeScript/Python/Java/Go. cdk diff = co się zmieni (jak terraform plan). cdk deploy = deploy.', how: 'npm install -g aws-cdk; cdk init app --language python/typescript', tips: ['cdk synth — wygeneruj CloudFormation template bez deploy', 'cdk bootstrap — jednorazowe przygotowanie konta AWS dla CDK (tworzy S3 bucket + rola)', 'cdk destroy — usuń wszystko co stworzył stack'] } },
    ]
  },
  {
    category: 'Różne — RDS, SQS, SSM',
    icon: '🔧',
    items: [
      { cmd: 'aws rds describe-db-instances --query "DBInstances[].[DBInstanceIdentifier,DBInstanceClass,DBInstanceStatus,Endpoint.Address,MultiAZ]" --output table', desc: 'Lista instancji RDS z endpointami',
        detail: { what: 'Wyświetla instancje RDS w regionie — identifier, typ, status, host do połączenia i czy Multi-AZ (HA).', how: 'aws rds describe-db-instances [--db-instance-identifier nazwa]', tips: ['aws rds create-db-snapshot --db-instance-identifier moja-db --db-snapshot-identifier snapshot-$(date +%Y%m%d)', 'aws rds restore-db-instance-from-db-snapshot — przywróć snapshot', 'aws rds modify-db-instance --db-instance-identifier nazwa --apply-immediately — zmień parametry'] } },
      { cmd: 'aws ssm get-parameter --name /prod/db/password --with-decryption --query Parameter.Value --output text', desc: 'Pobierz zaszyfrowany sekret z SSM Parameter Store',
        detail: { what: 'SSM Parameter Store = bezpieczne przechowywanie konfiguracji i sekretów. SecureString = szyfrowane KMS. --with-decryption = odszyfruj.', how: 'aws ssm put-parameter --name /prod/db/password --value "tajne" --type SecureString — zapisz', tips: ['aws ssm get-parameters-by-path --path /prod/ --recursive --with-decryption — pobierz cały folder', 'Hierarchia: /app/env/klucz — organizuj jak filesystem', 'Tańsze od Secrets Manager (0,05$/param vs 0,40$/sekret)'] } },
      { cmd: 'aws sqs send-message --queue-url https://sqs.eu-west-1.amazonaws.com/123456789/zamowienia.fifo --message-body \'{"order_id":42}\' --message-group-id "customer-1" --message-deduplication-id "order-42"', desc: 'Wyślij wiadomość na kolejkę SQS FIFO',
        detail: { what: 'SQS FIFO gwarantuje kolejność i dokładnie jedno dostarczenie. --message-group-id = kolejność w grupie. --message-deduplication-id = zapobiega duplikatom przez 5 minut.', how: 'SQS Standard: wysokie przepustowości, at-least-once. SQS FIFO: do 3000/s, exactly-once.', tips: ['aws sqs receive-message --queue-url [URL] --max-number-of-messages 10 --visibility-timeout 30 — odbierz i "zablokuj"', 'aws sqs delete-message --queue-url [URL] --receipt-handle [handle] — usuń po przetworzeniu', 'aws sqs get-queue-attributes --queue-url [URL] --attribute-names ApproximateNumberOfMessages — ile czeka'] } },
      { cmd: 'aws sns publish --topic-arn arn:aws:sns:eu-west-1:123:alerty --message "Deploy zakończony: v2.1.0 na prod" --subject "Deploy OK"', desc: 'Wyślij powiadomienie przez SNS (email/SMS/Lambda/SQS)',
        detail: { what: 'SNS = pub/sub messaging. Topic ma Subscriberów: email, SMS, HTTP endpoint, Lambda, SQS. Jeden publish → wszyscy subskrybenci dostają wiadomość.', how: 'aws sns subscribe --topic-arn [arn] --protocol email --notification-endpoint twoj@email.com — dodaj subskrybenta', tips: ['SNS → SQS = Fan-out pattern: jeden event → wiele kolejek → wiele konsumentów niezależnie', 'aws sns list-topics — lista tematów'] } },
    ]
  },
  {
    category: 'DynamoDB — NoSQL',
    icon: '⚡',
    items: [
      { cmd: 'aws dynamodb put-item --table-name Orders --item \'{"userId":{"S":"u123"},"orderId":{"S":"o456"},"status":{"S":"pending"},"total":{"N":"99.99"},"createdAt":{"S":"2024-01-15T10:00:00Z"}}\'', desc: 'Wstaw lub nadpisz item w DynamoDB',
        detail: { what: 'put-item zastępuje cały item jeśli PK/SK już istnieje. Użyj update-item jeśli chcesz zmienić tylko wybrane atrybuty.', how: 'Typy: S=String, N=Number, B=Binary, BOOL, NULL, L=List, M=Map, SS=StringSet, NS=NumberSet', flags: [{flag: '--condition-expression "attribute_not_exists(orderId)"', desc: 'Tylko jeśli nie istnieje (optimistic lock)'}, {flag: '--return-values ALL_OLD', desc: 'Zwróć poprzednią wartość itemu'}], tips: ['boto3: table.put_item(Item={...}) — bez typów (boto3 robi marshalling automatycznie)', 'Atomic counter: update-item z ADD #count :one'] } },
      { cmd: 'aws dynamodb get-item --table-name Orders --key \'{"userId":{"S":"u123"},"orderId":{"S":"o456"}}\' --projection-expression "orderId, #s, total" --expression-attribute-names \'{"#s":"status"}\'', desc: 'Pobierz konkretny item po kluczu (PK+SK)',
        detail: { what: 'Najszybsze i najtańsze zapytanie. Zawsze używaj get-item/query zamiast scan. Projection expression = pobierz tylko potrzebne atrybuty (redukuje koszty RCU).', how: '#s = alias dla zarezerwowanych słów (status). expression-attribute-names = mapa aliasów.', flags: [{flag: '--consistent-read', desc: 'Silnie spójny odczyt (2x droższy, ale zawsze najnowsze dane)'}], tips: ['batch-get-item — do 100 itemów naraz, do 16MB, dramatycznie tańsze niż 100x get-item', 'Domyślnie: eventually consistent (tańsze, wystarczające w 99% przypadków)'] } },
      { cmd: 'aws dynamodb query --table-name Orders --key-condition-expression "userId = :uid AND begins_with(orderId, :prefix)" --expression-attribute-values \'{"":{"S":"u123"},":prefix":{"S":"2024"}}\' --scan-index-forward false --limit 20', desc: 'Zapytaj o zamówienia użytkownika z 2024, posortowane malejąco',
        detail: { what: 'query = efektywne zapytanie po Partition Key + opcjonalnie Sort Key. scan-index-forward=false = sortowanie DESC (od najnowszego). limit = max zwracanych itemów.', how: 'key-condition-expression: tylko PK i SK operatory (=, <, >, between, begins_with)', flags: [{flag: '--filter-expression "total > :min"', desc: 'Filtruj po atrybutach (po odczytaniu! nie redukuje RCU)'}, {flag: '--index-name status-index', desc: 'Zapytaj przez GSI (Global Secondary Index)'}], tips: ['ExclusiveStartKey = paginacja: ostatni klucz z poprzedniej strony', 'ScanIndexForward=false + Limit=1 = najnowszy item użytkownika'] } },
      { cmd: 'aws dynamodb update-item --table-name Orders --key \'{"userId":{"S":"u123"},"orderId":{"S":"o456"}}\' --update-expression "SET #s = :new, updatedAt = :now ADD retries :one" --condition-expression "#s = :old" --expression-attribute-names \'{"#s":"status"}\' --expression-attribute-values \'{"":{"S":"completed"},":old":{"S":"pending"},":now":{"S":"2024-01-15T11:00:00Z"},":one":{"N":"1"}}\'', desc: 'Zaktualizuj status zamówienia (tylko jeśli pending) i inkrementuj counter',
        detail: { what: 'Atomowa aktualizacja. condition-expression = optimistic locking. SET = ustaw, ADD = inkrementuj/dodaj do setu, REMOVE = usuń atrybut, DELETE = usuń z setu.', how: 'Jeśli condition nie spełniona → ConditionalCheckFailedException', tips: ['UPDATE jest idempotentny z condition — safe dla retries', 'ADD #counter :one — atomic counter bez race condition'] } },
      { cmd: 'aws dynamodb batch-write-item --request-items file://batch.json', desc: 'Wstaw lub usuń do 25 itemów naraz (batch)',
        detail: { what: 'Do 25 operacji (PutRequest/DeleteRequest) naraz, max 16MB. Znacznie tańsze niż 25x put-item. Uwaga: brak transakcji — część może się nie udać (UnprocessedItems).', how: 'batch.json: {"Orders": [{"PutRequest": {"Item": {...}}}, {"DeleteRequest": {"Key": {...}}}]}', tips: ['transact-write-items — do 100 operacji ACID (transakcyjne, 2x droższe)', 'aws dynamodb scan --table-name Orders --parallel-scan 4 — równoległy pełny skan (dla migracji, nigdy w prod normalnie!)'] } },
    ]
  },
  {
    category: 'RDS & Aurora — Bazy SQL',
    icon: '🗄️',
    items: [
      { cmd: 'aws rds describe-db-instances --query "DBInstances[].[DBInstanceIdentifier,DBInstanceClass,DBInstanceStatus,Endpoint.Address,MultiAZ,StorageEncrypted]" --output table', desc: 'Lista instancji RDS z endpointami, Multi-AZ i szyfrowaniem',
        detail: { what: 'Wyświetla wszystkie instancje RDS w regionie z kluczowymi parametrami.', how: 'Dla Aurora: aws rds describe-db-clusters (cluster) i describe-db-instances (poszczególne nodes)', tips: ['aws rds describe-db-cluster-endpoints --db-cluster-identifier prod-aurora — endpointy read/write dla Aurora', 'writer endpoint = zawsze wskazuje na primary, reader endpoint = round-robin między replikami'] } },
      { cmd: 'aws rds create-db-snapshot --db-instance-identifier prod-db --db-snapshot-identifier "prod-db-before-migration-$(date +%Y%m%d-%H%M)"', desc: 'Utwórz snapshot RDS przed ryzykowną operacją',
        detail: { what: 'Snapshot = pełna kopia bazy w S3 (managed). Bez przestoju dla Multi-AZ (snapshot z secondary). Snapshot ma unikalną nazwę.', how: 'aws rds wait db-snapshot-completed --db-snapshot-identifier [nazwa] — czekaj na zakończenie', tips: ['aws rds describe-db-snapshots --db-instance-identifier prod-db — lista snapshotów instancji', 'Snapshoty automatyczne: do 35 dni PITR. Manualne: do ręcznego usunięcia.', 'aws rds copy-db-snapshot --source-db-snapshot-identifier arn:... --target-db-snapshot-identifier cross-region-backup --kms-key-id alias/aws/rds -- kopia do innego regionu'] } },
      { cmd: 'aws rds restore-db-instance-to-point-in-time --source-db-instance-identifier prod-db --target-db-instance-identifier prod-db-restored --restore-time 2024-01-15T10:30:00Z', desc: 'Przywróć bazę do konkretnego momentu w czasie (PITR)',
        detail: { what: 'Point-in-Time Recovery — przywraca do dowolnego momentu w oknie retencji (do 35 dni). Tworzy NOWĄ instancję (nie nadpisuje oryginalnej). Potem przekieruj connection string.', how: 'aws rds restore-db-instance-to-point-in-time --source-db-instance-identifier [źródło] --target-db-instance-identifier [nowa-nazwa] --restore-time [ISO8601]', tips: ['--use-latest-restorable-time — przywróć do ostatniego punktu', 'Dla Aurora: aws rds restore-db-cluster-to-point-in-time', 'PITR wymaga włączonego automated backup (backup-retention > 0)'] } },
      { cmd: 'aws rds modify-db-instance --db-instance-identifier prod-db --db-instance-class db.m5.large --allocated-storage 200 --apply-immediately', desc: 'Upgrade klasy instancji i rozszerzenie dysku',
        detail: { what: 'Zmiana klasy instancji = kilka minut przestoju (Multi-AZ = failover, <2min). Rozszerzenie storage = online bez przestoju. Storage nigdy nie można zmniejszyć!', how: '--apply-immediately = teraz. Bez tej flagi = następne maintenance window.', flags: [{flag: '--backup-retention-period 14', desc: 'Zmień retencję automatycznych backupów'}, {flag: '--enable-performance-insights', desc: 'Włącz Performance Insights (wolne zapytania)'}, {flag: '--enable-enhanced-monitoring', desc: 'OS-level metryki co 1-60 sekund'}], tips: ['aws rds describe-valid-db-instance-modifications --db-instance-identifier prod-db — sprawdź co można zmienić', 'Performance Insights: wizualizuje które zapytania żrą bazę'] } },
      { cmd: 'aws rds create-db-instance-read-replica --db-instance-identifier prod-db-replica --source-db-instance-identifier prod-db --db-instance-class db.t3.medium', desc: 'Utwórz Read Replica dla skalowania odczytów',
        detail: { what: 'Read Replica = asynchroniczna kopia tylko do odczytu. Odciąż primary. Możesz mieć do 15 replik. Replica w innym regionie = Disaster Recovery + globalne odczyty.', how: 'Aplikacja: skieruj SELECT zapytania na replica endpoint.', tips: ['aws rds promote-read-replica --db-instance-identifier prod-db-replica — promuj replikę do standalone (np. do migracji, separacji środowisk)', 'Replica lag: aws cloudwatch get-metric-statistics --namespace AWS/RDS --metric-name ReplicaLag'] } },
    ]
  },
  {
    category: 'Route 53 & DNS',
    icon: '🌐',
    items: [
      { cmd: 'aws route53 list-hosted-zones --query "HostedZones[].[Name,Id,Config.PrivateZone,ResourceRecordSetCount]" --output table', desc: 'Lista wszystkich hosted zones z liczbą rekordów',
        detail: { what: 'Wyświetla public i private hosted zones. Private hosted zones = DNS tylko wewnątrz VPC.', how: 'aws route53 get-hosted-zone --id /hostedzone/Z123 — szczegóły strefy', tips: ['aws route53 list-resource-record-sets --hosted-zone-id /hostedzone/Z123 — wszystkie rekordy DNS w strefie', 'aws route53 list-resource-record-sets --hosted-zone-id Z123 --query "ResourceRecordSets[?Type==\'A\']" — tylko A rekordy'] } },
      { cmd: 'aws route53 change-resource-record-sets --hosted-zone-id Z123 --change-batch \'{"Changes":[{"Action":"UPSERT","ResourceRecordSet":{"Name":"api.example.com","Type":"A","TTL":60,"ResourceRecords":[{"Value":"1.2.3.4"}]}}]}\'', desc: 'Dodaj lub zaktualizuj rekord DNS',
        detail: { what: 'Action: CREATE (błąd jeśli istnieje), DELETE, UPSERT (create or update — najczęściej używane). Zmiany propagują się do 60 sekund (TTL wpływa na cache resolver).', how: 'Dla Alias (ALB, CloudFront, S3): zamiast TTL i ResourceRecords użyj AliasTarget', tips: ['aws route53 wait resource-record-sets-changed --id /change/CHANGEINFO_ID — czekaj na propagację', 'Alias dla ALB: "AliasTarget":{"HostedZoneId":"Z3DZXE0Q79N41H","DNSName":"alb-xxx.elb.amazonaws.com","EvaluateTargetHealth":true}', 'Nie używaj TTL > 300 dla rekordów które często zmieniasz (np. blue/green switch)'] } },
      { cmd: 'aws route53 create-health-check --caller-reference $(date +%s) --health-check-config \'{"Type":"HTTPS","ResourcePath":"/health","FullyQualifiedDomainName":"api.example.com","Port":443,"RequestInterval":30,"FailureThreshold":3}\'', desc: 'Utwórz health check dla DNS failover',
        detail: { what: 'Health Check sprawdza endpoint co RequestInterval sekund. Jeśli FailureThreshold razy z rzędu fail → mark unhealthy → Route53 usuwa z Failover routing.', how: 'Typy: HTTP, HTTPS, TCP, HTTP_STR_MATCH, HTTPS_STR_MATCH (sprawdza ciało odpowiedzi), CALCULATED (kombinacja innych)', flags: [{flag: '"EnableSNI":true', desc: 'Wymagane dla HTTPS z SNI'}, {flag: '"Regions":["us-east-1","eu-west-1","ap-southeast-1"]', desc: 'Regiony z których są testy (min 3)'}], tips: ['aws route53 get-health-check-status --health-check-id [id] — obecny status', 'Alarm gdy health check fail: aws route53 list-health-checks → CloudWatch alarm na HealthCheckStatus metric'] } },
      { cmd: 'aws route53domains register-domain --domain-name moja-domena.pl --duration-in-years 1 --auto-renew --admin-contact file://contact.json --registrant-contact file://contact.json --tech-contact file://contact.json', desc: 'Zarejestruj domenę przez Route 53 Domains',
        detail: { what: 'Route 53 = registrar + DNS. Rejestracja domeny bezpośrednio przez AWS. Ceny: .com $12/rok, .io $39/rok, .pl $33/rok.', how: 'contact.json: {"FirstName":"Jan","LastName":"Kowalski","ContactType":"PERSON","AddressLine1":"ul. Przykładowa 1","City":"Warszawa","CountryCode":"PL","ZipCode":"00-001","PhoneNumber":"+48.500000000","Email":"jan@example.com"}', tips: ['aws route53domains list-domains — twoje domeny', 'aws route53domains get-domain-detail --domain-name moja-domena.pl — WHOIS info', 'aws route53domains transfer-domain-to-another-aws-account — transferuj między kontami AWS'] } },
    ]
  },
  {
    category: 'VPC & Networking',
    icon: '🌐',
    items: [
      { cmd: 'aws ec2 describe-vpcs --query "Vpcs[].[VpcId,CidrBlock,IsDefault,Tags[?Key==\'Name\'].Value|[0]]" --output table', desc: 'Lista VPC z CIDR i nazwami',
        detail: { what: 'Wyświetla wszystkie VPC w regionie. Każde konto ma jeden domyślny VPC (IsDefault=True) w każdym regionie.', how: 'aws ec2 describe-subnets --filters "Name=vpc-id,Values=vpc-xxx" — subnety VPC', tips: ['aws ec2 describe-internet-gateways --filters "Name=attachment.vpc-id,Values=vpc-xxx" — IGW przypisany do VPC', 'aws ec2 describe-route-tables --filters "Name=vpc-id,Values=vpc-xxx" — tablice routingu'] } },
      { cmd: 'aws ec2 create-vpc --cidr-block 10.0.0.0/16 --tag-specifications \'ResourceType=vpc,Tags=[{Key=Name,Value=prod-vpc},{Key=Env,Value=prod}]\'', desc: 'Utwórz nowy VPC z blokiem CIDR',
        detail: { what: 'VPC = izolowana sieć. CIDR /16 = 65536 adresów IP (10.0.0.0 - 10.0.255.255). Zarezerwowane przez AWS: pierwsze 4 i ostatni adres w każdym subnecie.', how: 'Po utworzeniu VPC: 1) attach IGW, 2) utwórz subnety, 3) skonfiguruj route tables, 4) utwórz security groups', flags: [{flag: '--enable-dns-support', desc: 'DNS resolution w VPC (domyślnie włączone)'}, {flag: '--enable-dns-hostnames', desc: 'Publiczne hostname dla EC2 (potrzebne dla ECS, EKS)'}], tips: ['Nie używaj 172.17.0.0/16 — to domyślna sieć Docker, konflikty', 'Planuj CIDRy! VPC peering wymaga nienachodząych się CIDR. 10.0.0.0/16, 10.1.0.0/16, 10.2.0.0/16...'] } },
      { cmd: 'aws ec2 create-subnet --vpc-id vpc-xxx --cidr-block 10.0.1.0/24 --availability-zone eu-west-1a --tag-specifications \'ResourceType=subnet,Tags=[{Key=Name,Value=public-1a},{Key=Type,Value=public}]\'', desc: 'Utwórz subnet w konkretnej Availability Zone',
        detail: { what: '/24 = 251 dostępnych adresów (5 zarezerwowanych przez AWS). Public subnet = z routem do IGW. Private = bez routu do IGW, ewentualnie przez NAT Gateway.', how: 'Po utworzeniu subnetu: aws ec2 modify-subnet-attribute --subnet-id subnet-xxx --map-public-ip-on-launch — instancje automatycznie dostają publiczne IP', tips: ['Dla produkcji: min 2 AZ na każdy tier (public/private/data)', 'aws ec2 describe-availability-zones --region eu-west-1 — dostępne AZ w regionie'] } },
      { cmd: 'aws ec2 create-security-group --group-name api-sg --description "API servers SG" --vpc-id vpc-xxx && aws ec2 authorize-security-group-ingress --group-id sg-xxx --protocol tcp --port 8000 --source-group sg-alb', desc: 'Utwórz Security Group i dodaj regułę z innej SG',
        detail: { what: 'Security Groups są stateful (odpowiedź na dozwolony ruch jest automatycznie dozwolona). Domyślnie: brak inbound, cały outbound dozwolony.', how: 'Najlepsza praktyka: reguły oparte na Security Groups (nie na IP). SG ALB → SG EC2 → SG RDS', flags: [{flag: '--protocol -1', desc: 'Wszystkie protokoły (używaj ostrożnie)'}, {flag: '--cidr 0.0.0.0/0', desc: 'Wszystkie IP (tylko dla publicznych LB, nigdy EC2)'}], tips: ['aws ec2 describe-security-groups --filters "Name=group-name,Values=api-sg" — znajdź SG po nazwie', 'aws ec2 describe-network-interfaces --filters "Name=group-id,Values=sg-xxx" — co używa tej SG'] } },
      { cmd: 'aws ec2 create-flow-logs --resource-ids vpc-xxx --resource-type VPC --traffic-type ALL --log-destination-type cloud-watch-logs --log-destination arn:aws:logs:eu-west-1:123:log-group:vpc-flow-logs --deliver-logs-permission-arn arn:aws:iam::123:role/FlowLogsRole', desc: 'Włącz VPC Flow Logs do CloudWatch',
        detail: { what: 'Flow Logs rejestruje cały ruch sieciowy w VPC/Subnet/ENI: source IP, dest IP, port, protokół, akcja (ACCEPT/REJECT), bajty, pakiety. Niezbędne do security audit i troubleshootingu.', how: '--traffic-type: ALL / ACCEPT / REJECT. Do S3 taniej niż CloudWatch (ale mniej wygodne do analizy)', tips: ['Athena query na Flow Logs w S3: znajdź kto łączy się z IP, jakie porty, odrzucone requesty', 'aws ec2 describe-flow-logs -- lista aktywnych flow logs', 'Koszt: $0.50/GB danych w CloudWatch, $0.25/GB do S3'] } },
    ]
  },
  {
    category: 'EKS & Kubernetes',
    icon: '☸️',
    items: [
      { cmd: 'eksctl create cluster --name prod --region eu-west-1 --nodegroup-name workers --node-type m5.large --nodes 3 --nodes-min 2 --nodes-max 10 --managed --with-oidc --alb-ingress-access', desc: 'Utwórz klaster EKS z managed nodegroup i OIDC (dla IAM Roles for Service Accounts)',
        detail: { what: '--managed = AWS zarządza nodami (patching, drain). --with-oidc = IRSA: Pod może mieć rolę IAM. --alb-ingress-access = uprawnienia dla AWS Load Balancer Controller.', how: 'Czas tworzenia: ~15-20 minut. eksctl tworzy stack CloudFormation.', flags: [{flag: '--spot', desc: 'Spot instances dla nodegroup (70% taniej, mogą być przerwane)'}, {flag: '--asg-access', desc: 'Uprawnienia dla Cluster Autoscaler'}, {flag: '--fargate', desc: 'Serverless nodes (Fargate Profiles zamiast EC2)'}], tips: ['eksctl create nodegroup --cluster prod --name gpu-workers --node-type p3.2xlarge — dodaj GPU nodegroup', 'eksctl scale nodegroup --cluster prod --name workers --nodes 5 — skaluj ręcznie'] } },
      { cmd: 'aws eks update-kubeconfig --name prod --region eu-west-1 --alias prod-cluster', desc: 'Zaktualizuj ~/.kube/config (dostęp kubectl do klastra)',
        detail: { what: 'Dodaje/aktualizuje kubeconfig z credentials dla klastra EKS. --alias = przyjazna nazwa zamiast długiego ARN.', how: 'Po wykonaniu: kubectl get nodes — sprawdź połączenie', tips: ['kubectl config get-contexts — lista klastrów w kubeconfig', 'kubectl config use-context prod-cluster — przełącz aktywny kontekst', 'kubectl config delete-context stary-klaster — usuń kontekst'] } },
      { cmd: 'aws eks list-addons --cluster-name prod && aws eks create-addon --cluster-name prod --addon-name aws-ebs-csi-driver --service-account-role-arn arn:aws:iam::123:role/EBSCSIRole', desc: 'Lista i instalacja EKS Add-onów (EBS CSI, CoreDNS, kube-proxy, VPC CNI)',
        detail: { what: 'Add-ony zarządzane przez AWS — automatyczne aktualizacje, AWS jest supportem. Kluczowe add-ony: vpc-cni (networking), kube-proxy, coredns, aws-ebs-csi-driver (persistent volumes), aws-efs-csi-driver.', how: 'aws eks describe-addon-versions --addon-name aws-ebs-csi-driver — dostępne wersje', tips: ['aws eks update-addon --cluster-name prod --addon-name aws-ebs-csi-driver --addon-version v1.xxx — upgrade add-on', 'IRSA (IAM Roles for Service Accounts): pod ma rolę IAM bez credentials w kontenerze'] } },
      { cmd: 'kubectl apply -f - <<\'EOF\'\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata:\n  name: api-hpa\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: api\n  minReplicas: 2\n  maxReplicas: 20\n  metrics:\n  - type: Resource\n    resource:\n      name: cpu\n      target:\n        type: Utilization\n        averageUtilization: 70\nEOF', desc: 'Utwórz HPA — auto-skalowanie podów na podstawie CPU',
        detail: { what: 'HPA (Horizontal Pod Autoscaler) skaluje liczbę podów. CPU > 70% → dodaj pody. CPU < 70% → usuń pody. Wymaga: metrics-server lub Prometheus Adapter.', how: 'Karpenter (node-level): gdy HPA chce więcej podów → Karpenter dodaje nowe EC2 nodes automatycznie.', tips: ['kubectl get hpa — status i current replicas', 'KEDA (Kubernetes Event-Driven Autoscaling): skaluj na podstawie SQS depth, Kinesis, custom metrics'] } },
    ]
  },
  {
    category: 'Secrets Manager & Parameter Store',
    icon: '🔒',
    items: [
      { cmd: 'aws secretsmanager create-secret --name prod/myapp/database --secret-string \'{"host":"db.xxx.eu-west-1.rds.amazonaws.com","port":5432,"username":"app_user","password":"SuperSecret123!"}\' --kms-key-id alias/prod-secrets', desc: 'Utwórz zaszyfrowany sekret (JSON z credentials DB)',
        detail: { what: 'Secrets Manager szyfruje sekret KMS. JSON pozwala trzymać wiele wartości w jednym sekrecie (host, port, user, password). Aplikacja deserializuje.', how: 'Sekret ma ARN: arn:aws:secretsmanager:eu-west-1:123:secret:prod/myapp/database-AbCdEf (suffix losowy)', flags: [{flag: '--description "Database credentials for prod"', desc: 'Opis sekretu'}, {flag: '--tags Key=Env,Value=prod', desc: 'Tagi dla cost allocation'}], tips: ['Hierarchia nazw: app/env/komponent — łatwe do listowania i uprawnień IAM', 'Lambda Layer: aws-secretsmanager-caching-python — cache sekretu lokalnie 5 minut, redukuje API calls'] } },
      { cmd: 'aws secretsmanager rotate-secret --secret-id prod/myapp/database --rotation-lambda-arn arn:aws:lambda:eu-west-1:123:function:rotate-db-creds --rotation-rules AutomaticallyAfterDays=30', desc: 'Włącz automatyczną rotację credentials co 30 dni',
        detail: { what: 'Rotacja: Lambda wywołuje nowe hasło w bazie i aktualizuje sekret. AWS ma gotowe Lambda rotators dla RDS (MySQL, PostgreSQL, Aurora) i Redshift.', how: 'aws secretsmanager describe-secret --secret-id prod/myapp/database — sprawdź konfigurację rotacji i ostatnią datę', tips: ['Dual-credentials rotation: przez chwilę oba hasła działają → zero downtime dla aplikacji', 'aws secretsmanager rotate-secret --secret-id xxx --rotate-immediately — wymuś rotację teraz'] } },
      { cmd: 'aws secretsmanager get-secret-value --secret-id prod/myapp/database --query SecretString --output text | python3 -c "import sys,json; d=json.load(sys.stdin); print(d[\'password\'])"', desc: 'Pobierz i wyciągnij konkretne pole z sekretu JSON',
        detail: { what: 'get-secret-value zwraca JSON string. --query SecretString wyciąga pole, python3 parsuje JSON i wyciąga konkretną wartość.', how: 'Wersjonowanie: --version-id lub --version-stage AWSPREVIOUS (poprzednia wersja podczas rotacji)', tips: ['W CI/CD: export DB_PASS=$(aws secretsmanager get-secret-value --secret-id prod/db --query SecretString --output text | jq -r .password)', 'Nigdy nie loguj sekretu! Maskuj w CI/CD (GitHub Secrets, GitLab masked variables)'] } },
      { cmd: 'aws ssm put-parameter --name /prod/app/feature-flag-new-ui --value "true" --type String --overwrite && aws ssm get-parameters-by-path --path /prod/app/ --recursive --output table', desc: 'Zapisz feature flag i wylistuj całą konfigurację aplikacji',
        detail: { what: 'SSM Parameter Store: String (plaintext), StringList, SecureString (encrypted KMS). Hierarchia /prod/app/ = jak folder.', how: 'Tańszy niż Secrets Manager: $0.00 do 10k Standard params/mies, $0.05/Advanced', tips: ['Parameter Store vs Secrets Manager: PS tańszy, brak auto-rotation. SM droższy, auto-rotation, wersjonowanie, cross-account.', 'aws ssm get-parameters-by-path --path /prod/ --recursive --with-decryption — pobierz wszystkie sekrety całej aplikacji'] } },
    ]
  },
  {
    category: 'Cost Explorer & Billing',
    icon: '💰',
    items: [
      { cmd: 'aws ce get-cost-and-usage --time-period Start=2024-01-01,End=2024-02-01 --granularity MONTHLY --metrics "BlendedCost" "UsageQuantity" --group-by Type=DIMENSION,Key=SERVICE --query "ResultsByTime[].Groups[].[Keys[0],Metrics.BlendedCost.Amount]" --output table', desc: 'Ile kosztuje każdy serwis AWS w tym miesiącu',
        detail: { what: 'Cost Explorer API zwraca koszty pogrupowane według wymiaru (SERVICE, REGION, LINKED_ACCOUNT, TAG).', how: '--granularity: DAILY, MONTHLY, HOURLY (tylko 14 dni historii)', flags: [{flag: '--filter \'{"Dimensions":{"Key":"SERVICE","Values":["Amazon EC2"]}}\' ', desc: 'Filtruj do konkretnego serwisu'}, {flag: '--group-by Type=TAG,Key=Project', desc: 'Grupuj po tagu (np. Project)'}], tips: ['aws ce get-cost-forecast — prognoza kosztów na następny miesiąc', 'Tagi: włącz Cost Allocation Tags w Billing Console → pojawią się w Cost Explorer'] } },
      { cmd: 'aws budgets create-budget --account-id 123456789 --budget \'{"BudgetName":"prod-monthly-limit","BudgetLimit":{"Amount":"500","Unit":"USD"},"TimeUnit":"MONTHLY","BudgetType":"COST","CostFilters":{"Service":["Amazon EC2"]}}\' --notifications-with-subscribers \'[{"Notification":{"NotificationType":"ACTUAL","ComparisonOperator":"GREATER_THAN","Threshold":80,"ThresholdType":"PERCENTAGE"},"Subscribers":[{"SubscriptionType":"EMAIL","Address":"devteam@firma.pl"}]},{"Notification":{"NotificationType":"FORECASTED","ComparisonOperator":"GREATER_THAN","Threshold":100,"ThresholdType":"PERCENTAGE"},"Subscribers":[{"SubscriptionType":"SNS","Address":"arn:aws:sns:..."}]}]\'', desc: 'Utwórz budżet z alertem przy 80% i prognozą > 100%',
        detail: { what: 'Dwa alerty: ACTUAL (już przekroczyłeś 80%) i FORECASTED (prognoza mówi że przekroczysz 100%). Subscriber SNS = automate response (Lambda wyłącza zasoby).', how: 'aws budgets describe-budgets --account-id 123456789 — lista budżetów', tips: ['Cost Anomaly Detection: aws ce create-anomaly-monitor — ML wykrywa nieoczekiwane wzrosty kosztów', 'Savings Plans: aws savingsplans describe-savings-plans — aktywne plany oszczędnościowe'] } },
      { cmd: 'aws ce get-savings-plans-purchase-recommendation --savings-plans-type COMPUTE_SP --term-in-years ONE_YEAR --payment-option NO_UPFRONT --lookback-period-in-days SIXTY_DAYS', desc: 'Rekomendacja zakupu Savings Plans na podstawie historii',
        detail: { what: 'AWS analizuje 60 dni historii i rekomenduje jaki Savings Plan (Compute, EC2, SageMaker) kupić żeby zaoszczędzić maksymalnie przy minimalnym ryzyku.', how: 'Typy: COMPUTE_SP (elastyczny, 66% taniej), EC2_INSTANCE_SP (konkretna rodzina, 72% taniej)', tips: ['Reserved Instances: aws ec2 describe-reserved-instances-offerings — dostępne RI', 'Spot: aws ec2 describe-spot-price-history --instance-type m5.large — historia cen Spot'] } },
    ]
  },
  {
    category: 'AWS CDK — Infrastructure as Code',
    icon: '🏗️',
    items: [
      { cmd: 'npm install -g aws-cdk && mkdir moj-projekt && cd moj-projekt && cdk init app --language python', desc: 'Zainstaluj CDK i utwórz nowy projekt Python',
        detail: { what: 'CDK generuje CloudFormation templates z kodu TypeScript/Python/Java/Go/C#. Pip install wymagany: pip install aws-cdk-lib constructs.', how: 'Struktura: app.py (entry), moj_projekt/moj_projekt_stack.py (Stack), cdk.json (config)', tips: ['cdk --version — sprawdź wersję', 'CDK v2 = jedna paczka aws-cdk-lib (vs v1 gdzie każdy serwis = osobna paczka)'] } },
      { cmd: 'cdk bootstrap aws://123456789/eu-west-1', desc: 'Bootstrap CDK — jednorazowe przygotowanie konta AWS (S3 + IAM)',
        detail: { what: 'Tworzy CDKToolkit stack: S3 bucket na assets (Lambda ZIPs, templates), IAM role dla deploymentu. Wymagane raz per account/region.', how: 'cdk bootstrap --profile prod — bootstrap dla konkretnego profilu', tips: ['Jeśli deploy w różnych regionach: cdk bootstrap aws://123/eu-west-1 aws://123/us-east-1', 'cdk bootstrap --trust [pipeline-account-id] — trust dla cross-account pipeline'] } },
      { cmd: 'cdk synth 2>&1 | head -50', desc: 'Wygeneruj CloudFormation template i podejrzyj pierwsze 50 linii',
        detail: { what: 'cdk synth = kompiluje CDK do CloudFormation YAML/JSON. Bez --output wyświetla na stdout. Dobry do sprawdzenia co zostanie zadeploy\'owane.', how: 'Wynik: cdk.out/MojProjektStack.template.json — pełny CloudFormation template', tips: ['cdk synth --no-lookups — synth bez odwołania do AWS API (szybsze w CI)', 'Błędy synth = błędy przed deploy, bezpieczne'] } },
      { cmd: 'cdk diff && cdk deploy --require-approval never --outputs-file cdk-outputs.json', desc: 'Pokaż różnice i wdróż infrastrukturę',
        detail: { what: 'cdk diff = co się zmieni (jak terraform plan). cdk deploy = deploy. --require-approval never = bez pytania (CI/CD). --outputs-file = zapisz Outputs stacka do pliku JSON.', how: 'cdk deploy "*" — deploy wszystkich stacków. cdk deploy StackA StackB — tylko wybrane.', flags: [{flag: '--context env=prod', desc: 'Przekaż context variable do kodu CDK'}, {flag: '--hotswap', desc: 'Szybki deploy tylko Lambdy/ECS bez pełnego CloudFormation (dev only!)'}], tips: ['cdk deploy --profile prod — deploy na produkcyjne konto', 'CDK Pipelines: cdk.pipelines.CodePipeline — self-mutating pipeline (aktualizuje się sam)'] } },
      { cmd: 'cdk destroy --force', desc: 'Usuń wszystkie zasoby stacku CDK',
        detail: { what: 'Usuwa CloudFormation stack i WSZYSTKIE zasoby w nim (EC2, RDS, S3 bucket jeśli RemovalPolicy=DESTROY). --force = bez pytania.', how: 'UWAGA: RemovalPolicy.RETAIN (domyślnie dla RDS, S3) = zasób zostaje po usunięciu stacku!', tips: ['aws cloudformation list-stacks — sprawdź czy stack usunięty', 'Dla baz danych: zmień RemovalPolicy na DESTROY tylko w dev/staging, nigdy w prod'] } },
    ]
  },

  {
    category: 'ECR — Docker Registry',
    icon: '🐳',
    items: [
      { cmd: 'aws ecr get-login-password --region eu-west-1 | docker login --username AWS --password-stdin 123456789.dkr.ecr.eu-west-1.amazonaws.com', desc: 'Zaloguj Docker do ECR (wymagane przed push/pull)',
        detail: { what: 'ECR = prywatny Docker registry na AWS. get-login-password zwraca tymczasowy token (12h). Bez tego docker push/pull zwroci Unauthorized.', how: 'Ustaw region i account ID. Token wazny 12h — odnow przed dlugotrwalymi pipeline.', tips: ['aws ecr get-login-password | docker login --username AWS --password-stdin $(aws sts get-caller-identity --query Account --output text).dkr.ecr.eu-west-1.amazonaws.com', 'W ECS/EKS task role = automatyczny dostep, nie trzeba recznie logowac'] } },
      { cmd: 'aws ecr create-repository --repository-name moja-aplikacja --image-scanning-configuration scanOnPush=true --encryption-configuration encryptionType=KMS', desc: 'Utworz repozytorium ECR z skanowaniem podatnosci',
        detail: { what: 'Tworzy prywatne repozytorium Docker. scanOnPush=true = Amazon Inspector skanuje kazdy push pod katem CVE. KMS szyfruje images at rest.', how: 'Repository URI: 123456789.dkr.ecr.eu-west-1.amazonaws.com/moja-aplikacja', tips: ['aws ecr describe-repositories — lista repozytoriow', 'aws ecr list-images --repository-name moja-aplikacja — lista tagow/digestow'] } },
      { cmd: 'docker build -t moja-aplikacja:v1.2.0 . && docker tag moja-aplikacja:v1.2.0 123456789.dkr.ecr.eu-west-1.amazonaws.com/moja-aplikacja:v1.2.0 && docker push 123456789.dkr.ecr.eu-west-1.amazonaws.com/moja-aplikacja:v1.2.0', desc: 'Build, tag i push do ECR',
        detail: { what: 'Trojkrok: build lokalny image, tag z pelnym URI rejestru ECR, push. Po pushu image dostepny dla ECS/EKS/Lambda w tym samym koncie i regionie.', how: 'Zawsze uzyj semantycznego wersjonowania dla prod. latest = antypattern w prod.', tips: ['Tez: docker build --platform linux/amd64 . — jesli budujesz na M1/M2 Mac dla EC2/ECS', 'ECR Lifecycle Policy: auto-usun stare tagi — nie plac za storage setek starych wersji'] } },
      { cmd: 'aws ecr put-lifecycle-policy --repository-name moja-aplikacja --lifecycle-policy file://ecr-lifecycle.json', desc: 'Lifecycle policy — zachowaj 10 ostatnich prod tagów, usun untagged po 1 dniu',
        detail: { what: 'Bez lifecycle policy ECR zapelnia sie gigabajtami starych imageow. $0.10/GB/mies = drogie. Policy automatycznie usuwa stare/nietagowane.', how: 'rules: priorytet nizszy = wazniejszy. tagStatus: tagged/untagged/any.', tips: ['aws ecr describe-images --repository-name moja-app --filter tagStatus=UNTAGGED — sprawdz ile untagged masz', 'Skanowanie: aws ecr describe-image-scan-findings --repository-name ... --image-id imageTag=v1.2.0'] } },
    ]
  },
  {
    category: 'ALB & API Gateway',
    icon: '⚖️',
    items: [
      { cmd: 'aws elbv2 create-load-balancer --name prod-alb --subnets subnet-1a subnet-1b --security-groups sg-xxx --type application', desc: 'Utworz Application Load Balancer',
        detail: { what: 'ALB (HTTP/HTTPS, layer 7) rozklada ruch miedzy target group. type=application dla HTTP/HTTPS, type=network dla TCP/UDP (NLB), type=gateway dla inline network appliances.', how: 'Po stworzeniu: utwórz listener (443 HTTPS) + target group + register targets', tips: ['aws elbv2 describe-load-balancers — lista ALB z DNS name', 'ALB DNS: prod-alb-xxx.eu-west-1.elb.amazonaws.com — podaj do Route53 lub CNAME'] } },
      { cmd: 'aws elbv2 create-target-group --name prod-tg --protocol HTTP --port 8080 --vpc-id vpc-xxx --health-check-path /health --health-check-interval-seconds 30 --healthy-threshold-count 2 --unhealthy-threshold-count 3', desc: 'Utworz target group z health checkiem',
        detail: { what: 'Target Group = logiczna grupa instancji/kontenerow/Lambda. ALB kieruje ruch do zdrowych targetow. Health check: /health zwracajacy 200 = zdrowy.', how: 'Typy targetow: instance (EC2), ip (ECS Fargate, on-premises), lambda (dla Lambda)', tips: ['aws elbv2 register-targets --target-group-arn arn:... --targets Id=i-xxx,Port=8080', 'aws elbv2 describe-target-health --target-group-arn arn:... — sprawdz ktore targety sa healthy'] } },
      { cmd: 'aws apigatewayv2 create-api --name prod-api --protocol-type HTTP --route-selection-expression "$request.method $request.path"', desc: 'Utworz HTTP API Gateway v2 (szybszy i tanszy niz REST API)',
        detail: { what: 'API Gateway v2 HTTP API = szybszy, do 71% tanszy niz REST API. Obsloguje routing, auth (JWT/Cognito/Lambda), CORS, throttling. Idealny jako proxy do Lambda lub HTTP endpoint.', how: 'Po stworzeniu: dodaj integracje (Lambda/HTTP) i route ($default lub GET /users/{id})', tips: ['aws apigatewayv2 get-apis — lista wszystkich API', 'Auto-deploy: aws apigatewayv2 create-stage --api-id xxx --stage-name prod --auto-deploy'] } },
      { cmd: 'aws apigatewayv2 create-integration --api-id xxx --integration-type AWS_PROXY --integration-uri arn:aws:lambda:...:moja-funkcja --payload-format-version 2.0 && aws apigatewayv2 create-route --api-id xxx --route-key "$default" --target integrations/yyy', desc: 'Polacz API Gateway z Lambda (catch-all route)',
        detail: { what: '$default route = przyjmuje kazda sciezke i metode HTTP. payload-format-version 2.0 = nowszy format eventu dla Lambdy (prostsza struktura niz v1.0).', how: 'Daj Lambdzie permission dla apigateway: aws lambda add-permission --action lambda:InvokeFunction --principal apigateway.amazonaws.com', tips: ['Custom domain: aws apigatewayv2 create-domain-name --domain-name api.twoja-domena.pl --domain-name-configurations CertificateArn=arn:acm:...', 'Throttling: aws apigatewayv2 create-stage z --default-route-settings ThrottlingBurstLimit=100,ThrottlingRateLimit=50'] } },
    ]
  },
  {
    category: 'CloudFront & WAF',
    icon: '🌐',
    items: [
      { cmd: 'aws cloudfront create-distribution --distribution-config file://cf-config.json', desc: 'Utworz dystrybucje CloudFront (CDN)',
        detail: { what: 'CloudFront = CDN z 450+ edge locations. Cachuje zawartosc blizej uzytkownikow. Typowe zastosowania: S3 static site, ALB frontend, API acceleration.', how: 'cf-config.json: Origins (S3/ALB), DefaultCacheBehavior (metody, TTL), PriceClass, Aliases (domeny)', tips: ['aws cloudfront create-invalidation --distribution-id Exx --paths "/*" — wyczysc cache po deploy', 'Distribution tworzy sie 15-20min — statusDeplayed = gotowe', 'aws cloudfront list-distributions --query "DistributionList.Items[].[Id,DomainName,Status]"'] } },
      { cmd: 'aws cloudfront create-invalidation --distribution-id E1EXAMPLE123456 --paths "/index.html" "/assets/*"', desc: 'Uniewaznij cache CloudFront po deploy',
        detail: { what: 'Invalidacja usuwa pliki z cache edge locations. Bez niej uzytkownicy dostaja stara wersje przez TTL (domyslnie 24h). Pierwsze 1000 invalidacji/mies gratis.', how: '/* = caly cache (drogie jesli duza dystrybucja, bo liczy per path). Uzywaj konkretnych sciezek w produkcji.', tips: ['Lepsze podejscie: wersjonuj pliki (main.abc123.js) i nie rób invalidacji — stary i nowy hash = inne URL = brak konfliktu', 'aws cloudfront get-invalidation --distribution-id E1xx --id Ixx — sprawdz status invalidacji'] } },
      { cmd: 'aws wafv2 create-web-acl --name prod-waf --scope CLOUDFRONT --region us-east-1 --default-action Allow={} --visibility-config SampledRequestsEnabled=true,CloudWatchMetricsEnabled=true,MetricName=prod-waf --rules file://waf-rules.json', desc: 'Utworz WAF ACL dla CloudFront (region MUSI byc us-east-1)',
        detail: { what: 'WAF (Web Application Firewall) chroni przed SQL injection, XSS, bots, DDoS aplikacyjny. CLOUDFRONT scope wymaga regionu us-east-1 (global).', how: 'Managed Rule Groups AWS: AWSManagedRulesCommonRuleSet, AWSManagedRulesBotControlRuleSet, AWSManagedRulesKnownBadInputsRuleSet', tips: ['aws wafv2 list-web-acls --scope CLOUDFRONT --region us-east-1 — lista WAF ACL', 'Rate limiting: RateBasedStatement (np. max 100 requestow/5min z jednego IP)', 'WAF dla ALB: --scope REGIONAL (bez --region us-east-1)'] } },
    ]
  },
  {
    category: 'SES — Email',
    icon: '📧',
    items: [
      { cmd: 'aws ses verify-domain-identity --domain twoja-domena.pl --region eu-west-1', desc: 'Zweryfikuj domene do wysylania emaili (zwraca token DNS)',
        detail: { what: 'SES musi zweryfikowac ze jestes wlascicielem domeny. Zwraca VerificationToken — dodaj jako DNS TXT record: _amazonses.twoja-domena.pl. Weryfikacja = kilka minut do kilku godzin.', how: 'aws ses get-domain-verification-attributes --domains twoja-domena.pl — sprawdz status', tips: ['Tez ustaw DKIM: aws ses put-email-identity-dkim-attributes — lepiej dostarczalnosc', 'Sandbox mode: mozesz wysylac tylko do zweryfikowanych adresow. Production access = wniosek w Support.'] } },
      { cmd: 'aws ses send-email --from noreply@domena.pl --to odbiorca@example.com --subject "Test" --text "Tresc" --region eu-west-1', desc: 'Wyslij szybki email testowy przez CLI',
        detail: { what: 'Szybki test ze CLI. W kodzie uzywaj boto3 lub SMTP. Jesli jestes w sandbox mode — odbiorca tez musi byc zweryfikowany.', how: 'aws ses send-email --from ... --destination ToAddresses=... --message Subject={Data=...},Body={Text={Data=...}}', tips: ['aws ses get-send-quota — sprawdz limit (domyslnie 200/dzien sandbox, 50k/dzien production)', 'aws ses get-send-statistics — bounce rate, complaint rate, delivery rate'] } },
      { cmd: 'aws ses put-configuration-set-event-destination --configuration-set-name transactional --event-destination Name=CloudWatch,Enabled=true,MatchingEventTypes=BOUNCE,COMPLAINT,DELIVERY,CloudWatchDestination={...}', desc: 'Konfiguruj monitoring email events (bounce/complaint/delivery)',
        detail: { what: 'Configuration Set sladzi zdarzenia dla wysylanych emaili: SEND, DELIVERY, BOUNCE (twardy/miekki), COMPLAINT, OPEN, CLICK, REJECT. Wysyla do CloudWatch/Kinesis/SNS.', how: 'Bounce rate > 10% = Amazon moze zawiesic konto SES. Complaint rate > 0.5% = ryzyko. Monitoruj!', tips: ['Suppression list: aws ses put-suppressed-destination — reczne dodanie adresu do blacklisty', 'aws ses list-suppressed-destinations — sprawdz aktualny suppression list'] } },
    ]
  },
  {
    category: 'Step Functions & EventBridge',
    icon: '🔄',
    items: [
      { cmd: 'aws stepfunctions create-state-machine --name order-processing --definition file://workflow.json --role-arn arn:aws:iam::123:role/StepFunctionsRole --type STANDARD', desc: 'Utworz Step Functions state machine (orkiestracja mikroserwisow)',
        detail: { what: 'Step Functions = serverless workflow orkiestrator. STANDARD: max 1 rok, audit trail, $0.025/1000 transitions. EXPRESS: max 5 min, wysoka throughput, $1/1M state transitions.', how: 'workflow.json: ASL (Amazon States Language) — JSON definiujacy stany, przejscia, retry, catch', tips: ['aws stepfunctions start-execution --state-machine-arn arn:... --input (JSON) — uruchom workflow', 'Console: Execution details = wizualny diagram z zaznaczonymi stanami i danymi'] } },
      { cmd: 'aws events put-rule --name every-5-minutes --schedule-expression "rate(5 minutes)" --state ENABLED && aws events put-targets --rule every-5-minutes --targets Id=lambda-target,Arn=arn:aws:lambda:...:moja-funkcja', desc: 'Harmonogram EventBridge — uruchom Lambda co 5 minut',
        detail: { what: 'EventBridge = managed event bus (nastepca CloudWatch Events). Schedules: rate(x minutes/hours/days) lub cron(min godz dzien-mies mies dzien-tyg rok).', how: 'Daj EventBridge permission do Lambda: aws lambda add-permission --principal events.amazonaws.com --action lambda:InvokeFunction', tips: ['aws events put-rule --schedule "cron(0 9 ? * MON-FRI *)" — o 9:00 w dni robocze', 'EventBridge Pipes: source (SQS/DynamoDB Stream) → filter → transform → Lambda/ECS — zero kodu, deklaratywnie'] } },
      { cmd: 'aws events put-rule --name order-created --event-pattern file://event-pattern.json --state ENABLED', desc: 'Event pattern rule — reaguj na custom events aplikacji',
        detail: { what: 'Custom event bus: aplikacje publishuja eventy (PutEvents), EventBridge routuje do odpowiednich targetow. Decoupling mikroserwisow — producentzy nie wiedza o konsumentach.', how: 'aws events put-events --entries file://events.json', tips: ['EventBridge Schema Registry: automatycznie odkrywa schematy eventow', 'aws events put-targets --rule order-created --targets Id=sns,Arn=arn:aws:sns:...:order-notifications — wyslij SNS'] } },
      { cmd: 'aws sqs create-queue --queue-name orders-dlq.fifo --attributes FifoQueue=true,ContentBasedDeduplication=true,MessageRetentionPeriod=1209600 && aws sqs create-queue --queue-name orders.fifo --attributes FifoQueue=true,ContentBasedDeduplication=true,RedrivePolicy={"deadLetterTargetArn":"arn:...orders-dlq.fifo","maxReceiveCount":"3"}', desc: 'Utwórz FIFO SQS z Dead Letter Queue',
        detail: { what: 'SQS FIFO: gwarantuje kolejnosc i exactly-once delivery. DLQ: wiadomosci ktore nie zostaly przetworzone po maxReceiveCount trafian do DLQ zamiast tracic.', how: 'FIFO wymaga nazwy z sufiksem .fifo. Standard queue: dowolna nazwa, higher throughput.', tips: ['aws sqs receive-message --queue-url https://sqs.eu-west-1.amazonaws.com/123/orders.fifo --max-number-of-messages 10', 'aws sqs get-queue-attributes --queue-url ... --attribute-names ApproximateNumberOfMessages — ile wiadomosci w kolejce'] } },
    ]
  },
  {
    category: 'Monitoring & Observability',
    icon: '📊',
    items: [
      { cmd: 'aws cloudwatch get-metric-statistics --namespace AWS/EC2 --metric-name CPUUtilization --dimensions Name=InstanceId,Value=i-xxx --start-time 2024-01-01T00:00:00 --end-time 2024-01-01T01:00:00 --period 300 --statistics Average', desc: 'Pobierz metryki CPU EC2 za ostatnia godzine',
        detail: { what: 'CloudWatch Metrics: dane liczbowe z AWS serwisow (CPU, Memory, Requests, Latency) lub custom (twoje aplikacje). Metryki sa archiwizowane przez 15 mies (rozdzielczosc spada z czasem).', how: 'period: rozdzielczosc w sekundach (60, 300, 3600). statistics: Average/Sum/Maximum/Minimum/SampleCount', tips: ['aws cloudwatch list-metrics --namespace AWS/Lambda — lista metryk dla Lambda', 'boto3: cloudwatch.put_metric_data — wyslij custom metryki (np. liczba zamowien, czas procesowania)'] } },
      { cmd: 'aws cloudwatch put-metric-alarm --alarm-name high-cpu --metric-name CPUUtilization --namespace AWS/EC2 --dimensions Name=InstanceId,Value=i-xxx --period 300 --threshold 80 --comparison-operator GreaterThanThreshold --evaluation-periods 3 --statistic Average --alarm-actions arn:aws:sns:...:ops-alerts --ok-actions arn:aws:sns:...:ops-alerts', desc: 'Alarm CloudWatch — alert gdy CPU > 80% przez 15 minut',
        detail: { what: 'Alarm triggeruje akcje (SNS, Auto Scaling, EC2 action) gdy metryka przekroczy prog przez evaluation-periods. 3 stany: OK, ALARM, INSUFFICIENT_DATA.', how: 'period*evaluation-periods = czas do triggerowania: 300*3 = 15 min. Zmniejsz aby alarm byl szybszy.', tips: ['aws cloudwatch describe-alarms --alarm-name-prefix prod- — lista alarmow', 'Composite Alarm: alarm ktory triggeruje gdy WIELE alarmow jest aktywnych jednoczesnie (redukuje false positives)'] } },
      { cmd: 'aws logs filter-log-events --log-group-name /aws/lambda/moja-funkcja --start-time $(($(date +%s) - 3600))000 --filter-pattern "ERROR" --query "events[*].[timestamp,message]" --output text | sort', desc: 'Znajdz ERROR logi Lambda z ostatniej godziny',
        detail: { what: 'CloudWatch Logs Insights to bardziej zaawansowany sposob analizy logow. filter-log-events = szybki CLI grep. Timestamp w milisekundach.', how: 'filter-pattern: "ERROR" — dowolne occurrence. "[ERROR]" — dokladne slowo. "{ $.statusCode = 500 }" — JSON field.', tips: ['aws logs start-query --log-group-name /aws/lambda/xxx --start-time ... --end-time ... --query-string "filter @message like /ERROR/ | stats count by bin(5m)" — CloudWatch Insights query', 'aws logs get-log-events --log-group /aws/lambda/xxx --log-stream-name "2024/01/01/[$LATEST]abc123" — konkretny log stream'] } },
      { cmd: 'aws xray get-service-graph --start-time $(date -d "1 hour ago" +%s) --end-time $(date +%s)', desc: 'X-Ray — mapa usług i latency distributed tracing',
        detail: { what: 'X-Ray zbiera traces z Lambda, ECS, EC2, API Gateway — pokazuje dokladny czas kazdego kroku requestu przez distributed system. Latency outliers, errors, throttles.', how: 'W Lambdzie wlacz tracing: aws lambda update-function-configuration --tracing-config Mode=Active', tips: ['aws xray get-trace-summaries --time-range-type TimeRangeByEdge --start-time ... --end-time ... --filter "responsetime > 1" — znajdz wolne requesty', 'Service Map w Console = wizualna mapa + procent bledow per serwis'] } },
    ]
  },
];
